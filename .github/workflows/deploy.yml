name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.24.5' # Using a known stable version

      - name: Enable Web Support
        run: flutter config --enable-web

      - name: Repair Web Project
        run: flutter create . --platforms web

      - name: Install dependencies
        run: flutter pub get

      - name: Build Web
        # Removed the problematic --web-renderer flag. 
        # Flutter 3.24+ will choose CanvasKit automatically for complex apps.
        run: flutter build web --release --base-href "/avatar/"

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web